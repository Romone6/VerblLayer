import { ConnectionStatus } from "@prisma/client";
import { prisma } from "@/lib/db";
import { getDevContext } from "@/lib/auth";
import { forbidden, notFound, ok, serverError } from "@/lib/http";
import { requirePermission } from "@/lib/permissions";
export async function POST(_: Request, { params }: { params: Promise<{ id: string }> }) { try { const { organisationId, userId, user } = await getDevContext(); requirePermission(user.role, "apps:manage"); const app = await prisma.app.findFirst({ where: { id: (await params).id, organisationId } }); if (!app) return notFound("App not found"); let error: string | null = null; try { const response = await fetch(`${app.baseUrl}/api/health`, { signal: AbortSignal.timeout(3000) }); if (!response.ok) error = `Health endpoint returned ${response.status}`; } catch (cause) { error = cause instanceof Error ? cause.message : String(cause); } const connectionStatus = error ? ConnectionStatus.failed : ConnectionStatus.connected; const updated = await prisma.app.update({ where: { id: app.id }, data: { connectionStatus } }); await prisma.auditLog.create({ data: { organisationId, eventType: "app_connection_tested", actorType: "user", actorId: userId, detailsJson: { app_id: app.id, connection_status: connectionStatus, error } } }); return ok({ app: updated, tested: true, connection_status: connectionStatus, error }); } catch (error) { return error instanceof Error && error.message.startsWith("Forbidden") ? forbidden(error.message) : serverError(error); } }
