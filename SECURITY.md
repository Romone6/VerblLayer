# Security policy

Do not file vulnerabilities in public issues. Send a minimal reproduction and impact summary to the repository maintainers through the contact method published with the repository.

The web console's local development authentication is not production-ready. Do not expose it publicly. Use a trusted external authentication and network boundary until a reviewed pluggable auth implementation is available.
